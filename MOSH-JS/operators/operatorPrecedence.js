let x = 2 + 3 * 4;
// Output = 14;

// In this case multiplication has higher precedence, if if we need to apply precedence we should put the expression in parenthesis

let x = (2 + 3) * 4;
// output = 20;