// In low level languages like console, c++ we need to allocate memmory for the variables. but in JS the JS engine automatically creates memmory and remove them when after use (Garbage collector)

const obj = {}
console.log(obj)